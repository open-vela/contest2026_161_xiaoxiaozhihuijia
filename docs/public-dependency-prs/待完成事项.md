# 发布后待完成事项

2026-09-18 已向五个公共仓发布六个 Draft PR，链接见本目录 README。已核对每个 PR 的目标分支、提交 SHA 和文件清单，未执行合并。

## 公共 PR 的实际阻塞

- 六个 PR 的 `cla/signature` 均报告提交使用 GitHub noreply 邮箱，要求可验证的公开提交邮箱。工作流中的 `cla-check` 成功不能覆盖这一独立状态错误。需确认贡献者签署 CLA 所用且允许公开的邮箱，按官方流程处理。
- Bluetooth PR #593 的 clang-format 检查失败，需定位并修复格式；不能据此断言业务编译失败。
- NuttX PR #388 的日志明确要求重新格式化 `tools/mkallsyms.py`，Python style check failed。
- NuttX PR #389 的检查指出新头文件的 include 不在 `Included Files` 区段。
- 其他构建任务尚未全部结束；六个 Draft PR 仍需维护者审核、必要验证及后续合入。

本轮发布不修改已交付固件、算法、BLE 行为或服务器开发工作区。运行 manifest 仍指向归档运行基线；新公共目标分支未完成整套构建与实机集成，不能直接替换运行版本。

## 作品最终收尾

- 主作品 PR #3 尚未合入；官网最终提交成功及回执尚未核实。
- 源码、bootfix 产物、DOCX、含硬件展示的视频、Skill 与已导出的 AI 日志已在作品 PR 中。日志是 30 个 Codex 会话、37,828 条事件的部分历史快照，不覆盖全部开发；Qoder 原始日志及全项目 AI 代码占比、Token 总量仍未完整核验。
- 全新环境复现、当前 scene9 的逐事件验收、Skill 的全新独立复现未完成。
- scene1/scene2 跳转问题保留为可修复未解决；Android BLE、精确同步、识别率与长期稳定性未完成验收，依团队要求保持冻结，不作为本轮自动修改范围。
- 第三方素材权利保留既有说明；团队原创代码采用 Apache 2.0 不代表第三方素材重新授权。

以上区分了提交材料、审核流程和已声明的验证边界；尚未验收的功能不应写成已完成。
