# 构建与使用说明

## 板端原有方式

本次唯一板端版本：rank1_v28_original_20260917。
服务器源码：/vdb/huangshan-motion-game-clean.20260901/workspaces/v28_original_20260917。
历史构建目录：/vdb/huangshan-motion-game-clean.20260901/cmake_out/v28_original_20260917。
原有CMake/Ninja方式，BOARD_CONFIG指向独立副本 build_config/board_config_i2c3，NUTTX_DIR指向该副本 nuttx。
当前既有构建入口（本轮未执行）：`ninja -C /vdb/huangshan-motion-game-clean.20260901/cmake_out/v28_original_20260917`。
这条命令会编译并改写产物；本说明不代表已取得本轮执行授权。

EXTRA_FLAGS：
`-DBLE_PROBE_SERIAL_ONLY=0 -DBLE_PROBE_ONBOARD_BEAT=1 -DBLE_PROBE_ONBOARD_BEAT_DIAG=1 -DBLE_PROBE_JOINT_UI_BLE=1 -DBLE_PROBE_CAPTURE_ACK_ONLY=0 -DSF32LB52_ACL_OBSERVER=0 -DHB_EVAL_STRIDE=2`
保持CONFIG_USEC_PER_TICK=10000、CONFIG_I2C=y、CONFIG_BT_BROADCASTER=y；ARM参数包含-mfpu=fpv5-sp-d16和-mfloat-abi=hard。

最新结果：const-allsyms修复后ARM/allsyms完成，最终ELF、BIN与System.map已纳入artifacts/v28_original。原1432字节SRAM溢出来自可写allsyms对象，mkallsyms.py现默认生成const声明和定义。原有链接布局保持不变。历史失败记录移入review_only，未删除。

BIN为2,557,620字节，SHA-256为77b8bb24c4e4dd136d0d6a406433986c06681edbb3ac2fb18c253d3ffd7bf5ea；build_id为rank1_v28_original_20260917。只确认构建产物，不宣称original已经启动、联调或兼容固定启动区。未经单独启动布局检查，不作为固定启动区烧录候选。

源码归档保留编译参与仓的源码、依赖版本、修改补丁、构建配置与最新成功构建日志。它是只读保全，不等于已经通过全新目录构建。源码标准化为source/apps、source/nuttx、source/vendor、source/external、source/frameworks等路径；恢复时需要按原manifest链接关系连接apps/external等路径。归档的CMakeCache/build.ninja包含历史绝对路径，仅供核验，不可直接搬到另一机器运行。
旧PR中的build_huangshan_motion_game.sh，以及fixed_boot/build_app.py，都不是本版本已验证的干净构建入口；本次不交付bootfix构建工具。

## 桌面源码

团结引擎1.10.0 / 2022.3.62t12，Python历史实测3.14；pyserial3.5已随工具携带；可选C#解析测试需要.NET SDK10（net10.0）。完整依赖由Packages/manifest.json与packages-lock.json记录。
新版运行入口为BteIntegration/README.md。COM7是历史端口，使用时核对实际端口；串口只能一个读取者。默认不加--start-app；不自动复位或烧录。
仅说明运行路径，本轮未启动串口、团结、摄像头或任何固件构建。
