# 仓库映射与复现边界

应用从归档source/apps/examples/openvela_ble_probe原样整理到app/openvela_ble_probe，manifest linkfile恢复原路径；CMakeLists.txt与Kconfig未改写。配置从build_config原样整理到board/v28_original，映射到SiFli板下独立配置名contest2026_161_v28_original，不覆盖默认nsh配置。

manifest保留openvela.xml原有项目列表，并以extend-project固定16个构建相关仓的归档提交。它不是全量仓库的revision锁文件。linkfile目标应不存在；如已有目录，先核对内容与备份，不自动覆盖。

本轮仅验证XML、映射源存在、目标唯一、固定提交格式、CMake引用源文件和归档哈希；未执行repo sync、链接创建、编译或烧录。标准build.sh新入口和完整依赖应用仍需独立干净环境验证。

dependencies/source_delta保留除团队应用外的公共仓新增／修改文件，patches保留已跟踪改动。评审时按仓库对应，避免重复应用。公共源码必须经过相应公共仓PR流程，不能把本地归档或linkfile当成公共修改已获审核。

docs/构建与使用说明.md保留历史构建取证信息，其中服务器路径不是本机运行命令。迁移路径：旧board_original/source中的应用现位于app，其余位于dependencies/source_delta；旧board_original/build_config现位于board/v28_original；旧desktop_project现位于desktop。不要运行旧PR的固定启动构建脚本来生成original。
