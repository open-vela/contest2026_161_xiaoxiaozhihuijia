# Codex 手动日志导出说明

小小指挥家队，手表应用创新赛道。用户授权合理修改后，对官方钩子新增独立 Codex rollout 解析适配；复用未修改官方 append_events 写入函数，使用未修改官方校验器及 schema，结果 ALL OK。

本批为此前保全的本机和服务器快照：37个源文件、36个会话；导出30个会话、37828条记录。6个会话因敏感信息候选或子任务范围整份排除，未删改保留会话正文。全部源文件哈希复核，导出记录逐条对应原始文件SHA256、行号与记录哈希；原始payload和时间全部一致。检查未发现已配置的敏感模式，但规则扫描不替代最终人工隐私审阅。

这是明确标记的手动历史适配，不是过去官方hook自动运行的证明，也不宣称组委会已经认可。未伪造工作区、cwd、模型、时间或Token统计。桌面会话使用schema最接近的partial模式，manifest明确标注其真实桌面来源，不能误认为VS Code采集。原始累计Token字段保存在metadata，不逐条求和。

主机端普通消息、工具调用和返回映射到相应字段；其他元数据、计数器和重复展示的event_msg保留为system记录。原始payload保留在metadata，避免丢失无法直接映射的字段。跨续接文件仅去除内容完全相同的重复记录，不删除同一文件内的重复事件。

本批只覆盖20260918T094209Z已选定快照，非全项目全集。后续录屏／提交会话、另外硬件任务的记录、Qoder CN不在本批中；即梦AI不属于AI Coding日志。完整日志缺口仍需继续归集。

logs/可作为单独日志增量供人工审阅；不要将私有原始会话、排除会话或整个manual_export目录上传。未修改服务器hook、账号密钥或GitHub。源码压缩包保持原样，本日志包是独立补充材料。

## 本次入仓更新

本批已获授权提交。实际日志按原始 UTC 日期分为54个文件，按日期拼接逐字节恢复原导出文件；manifest 指向会话最后日期文件，官方校验器汇总全部日期。再次校验 ALL OK。先前关于“未上传”和“独立待审阅包”的文字描述的是导出时状态。
