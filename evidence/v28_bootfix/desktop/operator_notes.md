# 2026-09-18 操作者记录

- 15:44:39 CST 开始本轮两小时预算；用户 Escape 暂停后于 16:00 左右明确恢复。
- 用户确认 v28 已烧录；本轮未操作固件、烧录工具、服务器或 GitHub。
- 15:55 COM7 可打开；捕获 NSH 提示符（含 ANSI 控制码），未发送启动命令。
- 第一桥接会话通过 STOP 文件正常结束，只关闭主机句柄。
- 16:00:28 第二桥接会话依据上一会话尚在5分钟有效期内的 NSH 原始证据发送一次授权启动命令。
- 捕获 READY session=29778，build_id=rank1_v28_bootfix_20260917，C 模式、diag=0、UI paused。
- 第一组用户反馈：约15次，小幅动作，“达不到明确判定要求”；动作数是估计，不能用于识别率统计。
- 第一组板端输出2条合法事件 (29778,1)、(29778,2)，桥接转发2、Unity接收2、处理0。原因是 Unity Mono Stopwatch 和 Windows QPC 原点不同，应用将时间检查拒绝。原始失败日志完整保留。
- 时钟修正采用双方直接调用 Windows QPC；这只是同一台电脑进程间计时，不是板端校时，不改变设备时间估计器。
- 团队 Word 文件可读取，本轮未修改；提取结果未发现可据以填写的成员分工，后续不得补猜。
- 修正后真实窗口一：event_id=3..6，4条串口合法事件、4条转发、4条接收、4条游戏处理；2 Perfect、2 Good，单局300分。
- 独立模拟测试端口9878，source=simulation。首次测试发送阶段完成、报告落盘因权限失败；Unity日志保留完整4次业务反馈、2次重复拒绝、1次坏CRC拒绝和1次过期拒绝。重新运行另一个模拟session并成功落盘；第二次处于菜单，处理结果为ignored_no_gameplay，不能作为游戏反馈证明。
- 重新恢复真实模式后，event_id=7..9，3条全部转发接收处理，反馈Good、Miss、Perfect，重新开始的一局150分。重连没有补发event_id=1..6。
- 后续用户补充：“这一轮运行我又进行了30拍左右。”记录为约30拍，无逐次时间标记，不强行分配到两个游戏子窗口；各窗口仅按板端输出事件统计，不计算识别率。16:26:02生成的旧审计尚未包含此补充，保留为历史版本，以后续审计为准。
- 完成电脑实机基本闭环；Android BLE、板端算法、长期稳定性、精确校时及服务器hook不在已验收范围。
